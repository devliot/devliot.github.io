---
phase: 08-ui-refresh
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ui-refresh.spec.ts
  - src/styles/reset.css
  - src/styles/footer.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "8 RED Playwright test stubs exist covering UI-01 through UI-04"
    - "The --color-border design token is available in the global :root"
    - "The footer background is white (#ffffff via --color-surface)"
  artifacts:
    - path: "tests/ui-refresh.spec.ts"
      provides: "RED Playwright E2E stubs for all 8 UI-refresh behaviors"
      min_lines: 80
    - path: "src/styles/reset.css"
      provides: "--color-border token in :root block"
      contains: "--color-border: #e5e5e5"
    - path: "src/styles/footer.css"
      provides: "White footer background"
      contains: "background-color: var(--color-surface)"
  key_links:
    - from: "tests/ui-refresh.spec.ts"
      to: "devliot-header"
      via: "Playwright locator on custom element"
      pattern: "page\\.locator\\('devliot-header'\\)"
    - from: "tests/ui-refresh.spec.ts"
      to: "devliot-footer"
      via: "Playwright locator on custom element"
      pattern: "page\\.locator\\('devliot-footer'\\)"
---

<objective>
Create the Playwright test scaffold (RED stubs) for all UI-refresh behaviors, add the --color-border design token to the palette, and change the footer to a white background.

Purpose: Wave 0 test stubs provide automated verification commands for all subsequent tasks. The token and footer changes are independent, low-risk modifications that can ship alongside the test scaffold without blocking the header refactor.

Output: `tests/ui-refresh.spec.ts` (8 RED test stubs), updated `src/styles/reset.css` (new token), updated `src/styles/footer.css` (white bg).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-refresh/08-CONTEXT.md
@.planning/phases/08-ui-refresh/08-RESEARCH.md
@.planning/phases/08-ui-refresh/08-PATTERNS.md
@.planning/phases/08-ui-refresh/08-VALIDATION.md
@.planning/phases/08-ui-refresh/08-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/styles/reset.css (lines 9-15 -- existing token block to extend):
```css
:root {
  /* Colors -- DEVLIOT brand (D-04) */
  --color-surface: #ffffff;
  --color-surface-alt: #f8f9fa;
  --color-accent: #333333;
  --color-text: #1a1a1a;
  --color-text-muted: #666666;
```

From src/styles/footer.css (line 5 -- the single line to change):
```css
  background-color: var(--color-surface-alt);
```

From tests/design-system.spec.ts (lines 1-7 -- Playwright test structure pattern):
```typescript
import { test, expect } from '@playwright/test';

const BREAKPOINTS = [
  { name: 'mobile', width: 375, height: 812 },
  { name: 'tablet', width: 768, height: 1024 },
  { name: 'desktop', width: 1280, height: 800 },
] as const;
```

From tests/design-system.spec.ts (lines 68-73 -- computed style assertion pattern):
```typescript
test('sticky header -- position is sticky', async ({ page }) => {
  await page.setViewportSize({ width: 375, height: 812 });
  await page.goto('/');
  const header = page.locator('devliot-header');
  await expect(header).toHaveCSS('position', 'sticky');
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright test stubs for UI-01 through UI-04 (RED)</name>
  <files>tests/ui-refresh.spec.ts</files>
  <read_first>
    - tests/design-system.spec.ts (Playwright structure pattern, computed style assertions)
    - tests/navigation-discovery.spec.ts (search button locator pattern, shadow DOM piercing)
    - .planning/phases/08-ui-refresh/08-VALIDATION.md (test map: exact test name patterns and automated commands)
    - .planning/phases/08-ui-refresh/08-RESEARCH.md (Q7 test patterns, Q2 scroll shadow assertion pattern)
    - .planning/phases/08-ui-refresh/08-PATTERNS.md (Playwright shadow DOM assertion pattern, article page wait pattern)
  </read_first>
  <action>
Create `tests/ui-refresh.spec.ts` with 8 test stubs that will FAIL (RED) against the current codebase. The tests assert the target state that Plan 02 will implement.

File structure:
```typescript
import { test, expect } from '@playwright/test';

const HOME_URL = '/';
const DEMO_ARTICLE_URL = '/article/01-demo-article';

test.describe('UI Refresh (Phase 8)', () => {
  // ... tests below
});
```

**Test 1 -- UI-01 white header background (per D-01):**
- Test name: `'UI-01: header background is white on both pages'`
- Navigate to `/`, assert `devliot-header` has `background-color` of `rgb(255, 255, 255)` via `toHaveCSS`.
- Navigate to `/article/01-demo-article`, wait for `devliot-article-page article h1` (timeout 10000), assert same.

**Test 2 -- UI-01 scroll shadow absent at top (per D-01):**
- Test name: `'UI-01: scroll shadow absent when page is at top'`
- Navigate to `/`, assert `devliot-header` has `box-shadow` of `none` via `toHaveCSS`.

**Test 3 -- UI-01 scroll shadow appears on scroll (per D-01):**
- Test name: `'UI-01: scroll shadow appears after scrolling'`
- Navigate to `/article/01-demo-article`, wait for `devliot-article-page article h1` (timeout 10000).
- Execute `window.scrollTo(0, 100)`.
- Wait 300ms (transition 0.2s + buffer).
- Read computed `boxShadow` via `page.locator('devliot-header').evaluate(el => getComputedStyle(el).boxShadow)`.
- Assert the value is NOT `'none'`.

**Test 4 -- UI-02 footer white background (per D-02):**
- Test name: `'UI-02: footer background is white'`
- Navigate to `/`, assert `devliot-footer` has `background-color` of `rgb(255, 255, 255)` via `toHaveCSS`.

**Test 5 -- UI-03 home header search, no logo (per D-04, D-08):**
- Test name: `'UI-03: home header has search affordance and no logo'`
- Navigate to `/`.
- Assert `page.locator('devliot-header .search-btn')` is visible.
- Assert `page.locator('devliot-header pre[aria-label="DEVLIOT"]')` has count 0.

**Test 6 -- UI-03 home header no hamburger (per D-09):**
- Test name: `'UI-03: home header has no hamburger menu button'`
- Navigate to `/`, set viewport 375x812 (mobile).
- Assert `page.locator('devliot-header button[aria-label="Ouvrir le menu"]')` has count 0.

**Test 7 -- UI-04 article header logo, no search (per D-06, D-08):**
- Test name: `'UI-04: article header has logo and no search'`
- Navigate to `/article/01-demo-article`, wait for `devliot-article-page article h1` (timeout 10000).
- Assert `page.locator('devliot-header pre[aria-label="DEVLIOT"]')` is visible.
- Assert `page.locator('devliot-header .search-btn')` has count 0.

**Test 8 -- UI-04 article header no hamburger (per D-09):**
- Test name: `'UI-04: article header has no hamburger menu button'`
- Navigate to `/article/01-demo-article`, wait for `devliot-article-page article h1` (timeout 10000), set viewport 375x812 (mobile).
- Assert `page.locator('devliot-header button[aria-label="Ouvrir le menu"]')` has count 0.

All 8 tests should FAIL against the current codebase (current header renders logo + hamburger on all pages, bg is `--color-surface-alt`).
  </action>
  <verify>
    <automated>cd /Users/eliott/dev/devliot && npx playwright test tests/ui-refresh.spec.ts --project=chromium 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/ui-refresh.spec.ts` exists and contains exactly 8 `test(` calls
    - Test names match VALIDATION.md grep patterns: `UI-01.*white`, `UI-01.*shadow.*absent`, `UI-01.*shadow.*appears`, `UI-02`, `UI-03.*search.*no logo`, `UI-03.*no hamburger`, `UI-04.*logo.*no search`, `UI-04.*no hamburger`
    - `grep -c "test\b(" tests/ui-refresh.spec.ts` returns `8`
    - Running `npx playwright test tests/ui-refresh.spec.ts --project=chromium` reports 8 tests, all failing (RED)
    - File imports `{ test, expect }` from `@playwright/test`
    - File uses `page.locator('devliot-header')` and `page.locator('devliot-footer')` patterns (not raw querySelector)
  </acceptance_criteria>
  <done>8 RED Playwright stubs exist in tests/ui-refresh.spec.ts matching the VALIDATION.md test map patterns, all failing against the current codebase</done>
</task>

<task type="auto">
  <name>Task 2: Add --color-border token and change footer to white background</name>
  <files>src/styles/reset.css, src/styles/footer.css</files>
  <read_first>
    - src/styles/reset.css (current :root token block, lines 9-15)
    - src/styles/footer.css (current :host block, lines 1-9)
    - .planning/phases/08-ui-refresh/08-CONTEXT.md (D-02 no separator, D-03 --color-border token)
    - .planning/phases/08-ui-refresh/08-UI-SPEC.md (Color section, --color-border value)
    - .planning/phases/08-ui-refresh/08-PATTERNS.md (footer.css single property change)
  </read_first>
  <action>
**reset.css change (per D-03):**
In `src/styles/reset.css`, add `--color-border: #e5e5e5;` after the `--color-text-muted: #666666;` line (line 15). The new line sits at line 16 inside the `:root` block. Do NOT modify any other tokens.

The `:root` colors block should read:
```css
:root {
  /* Colors -- DEVLIOT brand (D-04) */
  --color-surface: #ffffff;
  --color-surface-alt: #f8f9fa;
  --color-accent: #333333;
  --color-text: #1a1a1a;
  --color-text-muted: #666666;
  --color-border: #e5e5e5;
```

**footer.css change (per D-02):**
In `src/styles/footer.css` line 5, change:
```css
  background-color: var(--color-surface-alt);
```
to:
```css
  background-color: var(--color-surface);
```

This is the ONLY change in footer.css. No separator removal needed (D-02 confirms there is no existing separator). No other footer styles change.
  </action>
  <verify>
    <automated>cd /Users/eliott/dev/devliot && grep -- '--color-border' src/styles/reset.css && grep 'color-surface)' src/styles/footer.css && npx playwright test tests/ui-refresh.spec.ts -g "UI-02" --project=chromium 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep '--color-border: #e5e5e5' src/styles/reset.css` returns a match
    - `grep 'background-color: var(--color-surface);' src/styles/footer.css` returns a match
    - `src/styles/footer.css` does NOT contain `--color-surface-alt` (verify with `grep -c 'color-surface-alt' src/styles/footer.css` returns `0`)
    - `npx playwright test tests/ui-refresh.spec.ts -g "UI-02" --project=chromium` passes (footer is now white)
    - `npx playwright test tests/design-system.spec.ts --project=chromium` still passes (no design-system test depends on footer bg color)
  </acceptance_criteria>
  <done>--color-border token added to reset.css; footer background changed from --color-surface-alt to --color-surface; UI-02 test turns GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a CSS token and changes a CSS property value. No new trust boundaries introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | footer.css | accept | Footer displays only static copyright text "(c) 2026 DEVLIOT". Background color change introduces no new information surface. |
| T-08-02 | Tampering | reset.css | accept | CSS custom property --color-border is a presentation token with no security implications. It contains no user-controlled data. |

No new XSS surface, no new event handlers, no new fetch/network surface, no user-controlled data in any modified file.
</threat_model>

<verification>
- `npx playwright test tests/ui-refresh.spec.ts --project=chromium` -- 7 of 8 tests still RED (all except UI-02 which should be GREEN after footer change)
- `npx playwright test tests/design-system.spec.ts --project=chromium` -- all existing tests still GREEN
- `npx playwright test tests/navigation-discovery.spec.ts --project=chromium` -- all existing tests still GREEN
- `npx playwright test tests/deep-linkable-anchors.spec.ts --project=chromium` -- all 6 tests still GREEN
</verification>

<success_criteria>
- 8 Playwright test stubs exist in tests/ui-refresh.spec.ts matching VALIDATION.md patterns
- UI-02 test is GREEN (footer white background)
- All 7 remaining ui-refresh tests are RED (awaiting Plan 02 implementation)
- --color-border: #e5e5e5 token is declared in :root in reset.css
- No existing test suite regressions (47 existing tests remain GREEN)
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-refresh/08-01-SUMMARY.md`
</output>
